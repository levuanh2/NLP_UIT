"""Context builder test skeleton."""

import pytest


@pytest.mark.skip(reason="TODO(phase-implementation): implement context rendering")
def test_context_builder_preserves_legal_metadata() -> None:
    # Arrange / Act / Assert
    pytest.fail("Enable after legal context rendering is implemented.")
