"""Ingestion integration test skeleton."""

import pytest


@pytest.mark.skip(reason="TODO(phase-implementation): implement ingestion pipeline")
def test_ingestion_pipeline_preserves_legal_hierarchy() -> None:
    # Arrange / Act / Assert
    pytest.fail("Enable when ingestion orchestration is implemented.")
