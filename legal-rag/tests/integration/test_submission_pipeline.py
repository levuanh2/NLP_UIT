"""Submission pipeline integration test skeleton."""

import pytest


@pytest.mark.skip(reason="TODO(phase-implementation): wire submission service")
def test_submission_pipeline_writes_one_answer_per_question() -> None:
    # Arrange / Act / Assert
    pytest.fail("Enable when real question answering is wired.")
